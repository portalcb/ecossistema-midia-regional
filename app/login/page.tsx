import { loginAction } from './actions';
export default async function Login({searchParams}:{searchParams:Promise<{erro?:string}>}){const q=await searchParams;return <main className="login-page"><section className="login-card"><div className="brand"><span className="mark">MR</span><span>MÍDIA REGIONAL</span></div><span className="kicker">Gestão editorial</span><h1>Entrar no painel</h1><p className="muted">Acesso reservado à equipe autorizada.</p>{q.erro&&<div className="demo">E-mail ou senha inválidos.</div>}<form action={loginAction} className="login-form"><label>E-mail<input name="email" type="email" autoComplete="email" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required minLength={8}/></label><button className="btn dark" type="submit">Entrar</button></form></section></main>}
