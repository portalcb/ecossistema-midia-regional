import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const t=await sql`select coalesce(sum(case when entry_type='income' and status='paid' then amount else 0 end),0) received,coalesce(sum(case when entry_type='income' and status='pending' then amount else 0 end),0) receivable,coalesce(sum(case when entry_type='expense' and status='paid' then amount else 0 end),0) paid,coalesce(sum(case when entry_type='expense' and status='pending' then amount else 0 end),0) payable from financial_entries where organization_id=${u.organizationId}`;const c=await sql`select coalesce(sum(case when status='paid' then amount else 0 end),0) paid,coalesce(sum(case when status in('pending','approved') then amount else 0 end),0) pending from commissions where organization_id=${u.organizationId}`;const r=t[0]||{};return <section><div className="page-head"><div><span className="eyebrow">Fase 5 · Relatórios</span><h1>Relatório Financeiro</h1><p>Consolidação calculada somente com dados registrados no sistema.</p></div></div><div className="panel"><h2>Fluxo financeiro</h2><p>Recebido: R$ {Number(r.received||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</p><p>A receber: R$ {Number(r.receivable||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</p><p>Pago: R$ {Number(r.paid||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</p><p>A pagar: R$ {Number(r.payable||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</p></div><div className="panel"><h2>Comissões</h2><p>Pagas: R$ {Number(c[0]?.paid||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</p><p>Pendentes/aprovadas: R$ {Number(c[0]?.pending||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</p></div></section>}
