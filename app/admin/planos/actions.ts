'use server';import{redirect}from'next/navigation';import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export async function createPlan(f:FormData){const u=await requireSession();const name=String(f.get('name')||'').trim();const price=Number(f.get('price')||0);const interval=String(f.get('interval')||'month');if(!name||!Number.isFinite(price)||price<0)throw new Error('Plano inválido');if(!['month','year','one_time'].includes(interval))throw new Error('Periodicidade inválida');await sql`insert into subscription_plans(organization_id,name,price,interval) values(${u.organizationId},${name},${price},${interval})`;redirect('/admin/planos')}
