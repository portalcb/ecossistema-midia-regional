import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select s.id,s.name,s.email,s.status,count(sb.id)::int subscriptions from subscribers s left join subscriptions sb on sb.subscriber_id=s.id where s.organization_id=${u.organizationId} group by s.id order by s.created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 5 · Assinaturas</span><h1>Assinantes</h1><p>Cadastro e situação dos assinantes.</p></div></div><div className="panel">{rows.length===0?<p className="empty">Nenhum assinante cadastrado.</p>:rows.map((r:any)=><article key={r.id}><strong>{r.name}</strong><p>{r.email||'Sem e-mail'} · {r.status} · {r.subscriptions} assinatura(s)</p></article>)}</div></section>}
