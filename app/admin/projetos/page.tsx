import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';import{createProject}from'./actions';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select p.id,p.name,p.status,p.budget,p.starts_at,p.ends_at,c.name company from commercial_projects p left join crm_companies c on c.id=p.company_id where p.organization_id=${u.organizationId} and p.deleted_at is null order by p.created_at desc`;const companies=await sql`select id,name from crm_companies where organization_id=${u.organizationId} and deleted_at is null order by name`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Projetos especiais</span><h1>Projetos</h1><p>Projetos para municípios, cooperativas, propriedades, indústrias e empresas.</p></div></div><div className="panel"><h2>Novo projeto</h2><form action={createProject}><input name="name" required placeholder="Nome do projeto"/> <select name="company_id"><option value="">Cliente / parceiro</option>{companies.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select> <input name="budget" type="number" min="0" step="0.01" placeholder="Orçamento"/> <input name="starts_at" type="date"/> <input name="ends_at" type="date"/> <button type="submit">Criar projeto</button></form></div><div className="panel">{rows.length===0?<p className="empty">Nenhum projeto comercial cadastrado.</p>:<div className="table-wrap"><table><thead><tr><th>Projeto</th><th>Cliente</th><th>Status</th><th>Orçamento</th><th>Período</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.name}</strong></td><td>{r.company||'—'}</td><td>{r.status}</td><td>R$ {Number(r.budget).toLocaleString('pt-BR',{minimumFractionDigits:2})}</td><td>{r.starts_at||'—'} → {r.ends_at||'—'}</td></tr>)}</tbody></table></div>}</div></section>}
