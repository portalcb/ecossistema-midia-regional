import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select id,name,tier,status,starts_at,ends_at from sponsors where organization_id=${u.organizationId} order by created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Parcerias</span><h1>Patrocinadores</h1><p>Gestão de patrocínios com período, categoria e status.</p></div></div><div className="panel">{rows.length===0?<p className="empty">Nenhum patrocinador cadastrado.</p>:rows.map((r:any)=><article key={r.id}><strong>{r.name}</strong><p>{r.tier||'Sem categoria'} · {r.status}</p></article>)}</div></section>}
