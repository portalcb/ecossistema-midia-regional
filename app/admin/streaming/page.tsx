import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';import{createSeries,createSeason,createEpisode}from'./actions';export const dynamic='force-dynamic';
export default async function Page(){const u=await requireSession();const series=await sql`select s.id,s.title,s.status,s.premium,count(distinct se.id)::int seasons,count(distinct e.id)::int episodes from streaming_series s left join streaming_seasons se on se.series_id=s.id left join streaming_episodes e on e.season_id=se.id where s.organization_id=${u.organizationId} group by s.id order by s.created_at desc`;const seasons=await sql`select se.id,se.title,se.season_number,s.title series from streaming_seasons se join streaming_series s on s.id=se.series_id where se.organization_id=${u.organizationId} order by s.title,se.season_number`;return <section><div className="page-head"><div><span className="eyebrow">Fase 6 · Streaming</span><h1>Catálogo Premium</h1><p>Administração de séries, temporadas e episódios. Provedor de vídeo externo ainda não ativado.</p></div></div><div className="panel"><h2>Nova série</h2><form action={createSeries}><input name="title" placeholder="Título da série" required/><input name="description" placeholder="Descrição"/><button type="submit">Criar série</button></form></div><div className="panel"><h2>Nova temporada</h2><form action={createSeason}><select name="series_id" required><option value="">Selecione a série</option>{series.map((s:any)=><option key={s.id} value={s.id}>{s.title}</option>)}</select><input name="season_number" type="number" min="1" placeholder="Temporada" required/><input name="title" placeholder="Título da temporada" required/><button type="submit">Criar temporada</button></form></div><div className="panel"><h2>Novo episódio</h2><form action={createEpisode}><select name="season_id" required><option value="">Selecione a temporada</option>{seasons.map((s:any)=><option key={s.id} value={s.id}>{s.series} · T{s.season_number} · {s.title}</option>)}</select><input name="episode_number" type="number" min="1" placeholder="Episódio" required/><input name="title" placeholder="Título do episódio" required/><button type="submit">Criar episódio</button></form><p><strong>Vídeo:</strong> o cadastro do asset/playback será habilitado quando o provedor premium for aprovado.</p></div><div className="panel"><h2>Catálogo</h2>{series.length===0?<p className="empty">Nenhuma série cadastrada.</p>:series.map((s:any)=><article key={s.id}><strong>{s.title}</strong><p>{s.seasons} temporada(s) · {s.episodes} episódio(s) · {s.status} · {s.premium?'Premium':'Livre'}</p></article>)}</div></section>}
