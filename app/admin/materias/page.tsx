import Link from 'next/link'; import {db} from '@/lib/db'; import {requireSession} from '@/lib/auth';
export default async function Articles(){const s=await requireSession();const rows=await db()`select id,title,slug,status,updated_at from articles where organization_id=${s.organizationId} and deleted_at is null order by updated_at desc limit 100`;return <section className="panel"><div className="section-head"><div><span className="muted">Fluxo editorial</span><h1>Matérias</h1></div><Link className="btn dark" href="/admin/materias/nova">+ Nova matéria</Link></div><table className="table"><thead><tr><th>Título</th><th>Status</th><th>Atualização</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><Link href={`/admin/materias/${r.id}`}>{r.title}</Link></td><td><span className="status">{r.status}</span></td><td>{new Date(r.updated_at).toLocaleDateString('pt-BR')}</td></tr>)}</tbody></table>{!rows.length&&<p className="muted">Nenhuma matéria cadastrada.</p>}</section>}
