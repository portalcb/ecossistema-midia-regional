import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';import{createCoupon}from'./actions';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select id,code,discount_type,discount_value,redemptions,max_redemptions,active from coupons where organization_id=${u.organizationId} order by created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 5 · Assinaturas</span><h1>Cupons</h1><p>Descontos com limite de utilização.</p></div></div><div className="panel"><h2>Novo cupom</h2><form action={createCoupon}><input name="code" required placeholder="Código"/><select name="discount_type"><option value="percent">Percentual</option><option value="fixed">Valor fixo</option></select><input name="discount_value" type="number" min="0" step="0.01" required placeholder="Desconto"/><input name="max_redemptions" type="number" min="1" placeholder="Limite de usos"/><button type="submit">Criar cupom</button></form></div><div className="panel">{rows.length===0?<p className="empty">Nenhum cupom cadastrado.</p>:rows.map((r:any)=><article key={r.id}><strong>{r.code}</strong><p>{r.discount_value} {r.discount_type==='percent'?'%':'BRL'} · {r.redemptions}/{r.max_redemptions??'∞'} · {r.active?'Ativo':'Inativo'}</p></article>)}</div></section>}
