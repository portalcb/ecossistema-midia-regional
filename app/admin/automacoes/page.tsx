import { requireSession } from '@/lib/auth';
import { sql } from '@/lib/db';
export const dynamic='force-dynamic';
export default async function AutomacoesPage(){const user=await requireSession();const rules=await sql`select id,name,trigger_type,action_type,active,requires_human_approval,updated_at from automation_rules where organization_id=${user.organizationId} order by created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 3 · Automação</span><h1>Automações Editoriais</h1><p>Regras assistidas para reduzir trabalho repetitivo sem perder controle editorial.</p></div></div><div className="panel"><p className="empty">Por segurança editorial, novas regras começam desativadas e exigem aprovação humana por padrão.</p>{rules.length>0&&<div className="table-wrap"><table><thead><tr><th>Regra</th><th>Gatilho</th><th>Ação</th><th>Aprovação</th><th>Status</th></tr></thead><tbody>{rules.map((r:any)=><tr key={r.id}><td><strong>{r.name}</strong></td><td>{r.trigger_type}</td><td>{r.action_type}</td><td>{r.requires_human_approval?'Obrigatória':'Não'}</td><td>{r.active?'Ativa':'Desativada'}</td></tr>)}</tbody></table></div>}</div></section>}
