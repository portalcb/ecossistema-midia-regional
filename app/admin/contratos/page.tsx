import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';import{createContract}from'./actions';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select ct.id,ct.number,ct.title,ct.status,ct.value,ct.starts_at,ct.ends_at,c.name company from contracts ct left join crm_companies c on c.id=ct.company_id where ct.organization_id=${u.organizationId} order by ct.created_at desc`;const companies=await sql`select id,name from crm_companies where organization_id=${u.organizationId} and deleted_at is null order by name`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Comercial</span><h1>Contratos</h1><p>Contratos vinculados às empresas e projetos comerciais.</p></div></div><div className="panel"><h2>Novo contrato</h2><form action={createContract}><input name="title" required placeholder="Título do contrato"/><select name="company_id"><option value="">Empresa</option>{companies.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input name="value" type="number" min="0" step="0.01" placeholder="Valor"/><label>Início <input name="starts_at" type="date"/></label><label>Fim <input name="ends_at" type="date"/></label><button type="submit">Criar contrato</button></form></div><div className="panel">{rows.length===0?<p className="empty">Nenhum contrato cadastrado.</p>:<div className="table-wrap"><table><thead><tr><th>Nº</th><th>Contrato</th><th>Empresa</th><th>Valor</th><th>Vigência</th><th>Status</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.number}</td><td><strong>{r.title}</strong></td><td>{r.company||'—'}</td><td>R$ {Number(r.value).toLocaleString('pt-BR',{minimumFractionDigits:2})}</td><td>{r.starts_at||'—'} até {r.ends_at||'—'}</td><td>{r.status}</td></tr>)}</tbody></table></div>}</div></section>}
