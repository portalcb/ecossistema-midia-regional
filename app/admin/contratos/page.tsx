import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select ct.id,ct.number,ct.title,ct.status,ct.value,ct.starts_at,ct.ends_at,c.name company from contracts ct left join crm_companies c on c.id=ct.company_id where ct.organization_id=${u.organizationId} order by ct.created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Comercial</span><h1>Contratos</h1><p>Contratos vinculados a empresas, propostas e projetos.</p></div></div><div className="panel">{rows.length===0?<p className="empty">Nenhum contrato cadastrado.</p>:<div className="table-wrap"><table><thead><tr><th>Nº</th><th>Contrato</th><th>Empresa</th><th>Valor</th><th>Vigência</th><th>Status</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.number}</td><td><strong>{r.title}</strong></td><td>{r.company||'—'}</td><td>R$ {Number(r.value).toLocaleString('pt-BR',{minimumFractionDigits:2})}</td><td>{r.starts_at||'—'} → {r.ends_at||'—'}</td><td>{r.status}</td></tr>)}</tbody></table></div>}</div></section>}
