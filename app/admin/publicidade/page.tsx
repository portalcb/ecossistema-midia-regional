import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select a.id,a.name,a.placement,a.status,a.starts_at,a.ends_at,a.impressions,a.clicks,c.name company from ad_campaigns a left join crm_companies c on c.id=a.company_id where a.organization_id=${u.organizationId} order by a.created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Monetização</span><h1>Publicidade</h1><p>Campanhas, posições, períodos, criativos e métricas claramente separados do editorial.</p></div></div><div className="panel">{rows.length===0?<p className="empty">Nenhuma campanha publicitária cadastrada.</p>:<div className="table-wrap"><table><thead><tr><th>Campanha</th><th>Anunciante</th><th>Posição</th><th>Status</th><th>Impressões</th><th>Cliques</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.name}</strong></td><td>{r.company||'—'}</td><td>{r.placement}</td><td>{r.status}</td><td>{String(r.impressions)}</td><td>{String(r.clicks)}</td></tr>)}</tbody></table></div>}</div></section>}
