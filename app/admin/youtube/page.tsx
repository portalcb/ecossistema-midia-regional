import { requireSession } from '@/lib/auth';
import { sql } from '@/lib/db';
export const dynamic='force-dynamic';
export default async function YoutubePage(){const user=await requireSession();const [channel]=await sql`select channel_title,channel_url,status,last_synced_at from youtube_channels where organization_id=${user.organizationId} limit 1`;const videos=await sql`select id,title,youtube_video_id,published_at,import_status from public_videos_phase3 where organization_id=${user.organizationId} order by published_at desc nulls last limit 50`;return <section><div className="page-head"><div><span className="eyebrow">Fase 3 · Distribuição</span><h1>YouTube</h1><p>Importação editorial preparada para canal autorizado. Todo vídeo entra como rascunho e exige revisão humana.</p></div></div><div className="panel"><h2>{channel?.channel_title||'Canal ainda não conectado'}</h2><p>Status: {channel?.status||'Pendente de credenciais/autorização'}</p><p className="empty">Nenhuma chave é exposta no frontend e não há publicação automática.</p></div><div className="panel"><h2>Fila de vídeos</h2>{videos.length===0?<p className="empty">Nenhum vídeo importado.</p>:<div className="table-wrap"><table><thead><tr><th>Vídeo</th><th>Data</th><th>Status editorial</th></tr></thead><tbody>{videos.map((v:any)=><tr key={v.id}><td><strong>{v.title}</strong><br/><small>{v.youtube_video_id}</small></td><td>{v.published_at?new Date(v.published_at).toLocaleDateString('pt-BR'):'—'}</td><td>{v.import_status}</td></tr>)}</tbody></table></div>}</div></section>}
