import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select o.id,o.status,o.total,o.currency,o.provider,o.external_order_id,s.name subscriber,p.name plan from checkout_orders o left join subscribers s on s.id=o.subscriber_id left join subscription_plans p on p.id=o.plan_id where o.organization_id=${u.organizationId} order by o.created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 5 · Pagamentos</span><h1>Checkout e Pedidos</h1><p>Pedidos internos e integração futura com gateway aprovado.</p></div></div><div className="panel"><p><strong>Gateway:</strong> não configurado. Nenhuma cobrança externa é criada automaticamente.</p></div><div className="panel">{rows.length===0?<p className="empty">Nenhum pedido registrado.</p>:rows.map((r:any)=><article key={r.id}><strong>{r.subscriber||'Pedido'} · {r.plan||'Sem plano'}</strong><p>R$ {Number(r.total).toLocaleString('pt-BR',{minimumFractionDigits:2})} · {r.status} · {r.provider||'sem gateway'}</p></article>)}</div></section>}
