import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select id,beneficiary_name,commission_type,rate,amount,status from commissions where organization_id=${u.organizationId} order by created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 5 · Financeiro</span><h1>Comissões</h1><p>Comissões vinculáveis aos pedidos e parceiros.</p></div></div><div className="panel">{rows.length===0?<p className="empty">Nenhuma comissão registrada.</p>:rows.map((r:any)=><article key={r.id}><strong>{r.beneficiary_name}</strong><p>R$ {Number(r.amount).toLocaleString('pt-BR',{minimumFractionDigits:2})} · {r.status}</p></article>)}</div></section>}
