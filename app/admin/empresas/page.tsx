import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select id,name,company_type,status,email,phone,city,state from crm_companies where organization_id=${u.organizationId} and deleted_at is null order by name`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Relacionamento</span><h1>Empresas e Parceiros</h1><p>Cadastro central de prospects, clientes, parceiros, anunciantes e patrocinadores.</p></div></div><div className="panel">{rows.length===0?<p className="empty">Nenhuma empresa cadastrada.</p>:<div className="table-wrap"><table><thead><tr><th>Empresa</th><th>Tipo</th><th>Contato</th><th>Cidade</th><th>Status</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.name}</strong></td><td>{r.company_type}</td><td>{r.email||r.phone||'—'}</td><td>{[r.city,r.state].filter(Boolean).join(' / ')||'—'}</td><td>{r.status}</td></tr>)}</tbody></table></div>}</div></section>}
