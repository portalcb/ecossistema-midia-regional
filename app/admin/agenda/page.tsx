import { requireSession } from '@/lib/auth';
import { sql } from '@/lib/db';
export const dynamic='force-dynamic';
export default async function AgendaPage(){const user=await requireSession();const events=await sql`select id,title,event_type,starts_at,ends_at,location from editorial_events where organization_id=${user.organizationId} order by starts_at asc limit 100`;return <section><div className="page-head"><div><span className="eyebrow">Fase 3 · Planejamento</span><h1>Agenda Editorial</h1><p>Publicações, gravações, entrevistas, eventos e prazos em uma agenda única.</p></div></div><div className="panel">{events.length===0?<p className="empty">Nenhum compromisso editorial cadastrado.</p>:<div className="table-wrap"><table><thead><tr><th>Data</th><th>Compromisso</th><th>Tipo</th><th>Local</th></tr></thead><tbody>{events.map((e:any)=><tr key={e.id}><td>{new Date(e.starts_at).toLocaleString('pt-BR')}</td><td><strong>{e.title}</strong></td><td>{e.event_type}</td><td>{e.location||'—'}</td></tr>)}</tbody></table></div>}</div></section>}
