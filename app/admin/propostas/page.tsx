import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select p.id,p.number,p.title,p.status,p.total_value,p.valid_until,c.name company from proposals p left join crm_companies c on c.id=p.company_id where p.organization_id=${u.organizationId} order by p.created_at desc`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Comercial</span><h1>Propostas</h1><p>Controle de propostas, valores, validade e vínculo com oportunidades.</p></div></div><div className="panel">{rows.length===0?<p className="empty">Nenhuma proposta cadastrada.</p>:<div className="table-wrap"><table><thead><tr><th>Nº</th><th>Proposta</th><th>Empresa</th><th>Valor</th><th>Validade</th><th>Status</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.number}</td><td><strong>{r.title}</strong></td><td>{r.company||'—'}</td><td>R$ {Number(r.total_value).toLocaleString('pt-BR',{minimumFractionDigits:2})}</td><td>{r.valid_until||'—'}</td><td>{r.status}</td></tr>)}</tbody></table></div>}</div></section>}
