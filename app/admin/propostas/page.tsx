import{requireSession}from'@/lib/auth';import{sql}from'@/lib/db';import{createProposal}from'./actions';export const dynamic='force-dynamic';export default async function Page(){const u=await requireSession();const rows=await sql`select p.id,p.number,p.title,p.status,p.total_value,p.valid_until,c.name company from proposals p left join crm_companies c on c.id=p.company_id where p.organization_id=${u.organizationId} order by p.created_at desc`;const companies=await sql`select id,name from crm_companies where organization_id=${u.organizationId} and deleted_at is null order by name`;return <section><div className="page-head"><div><span className="eyebrow">Fase 4 · Comercial</span><h1>Propostas</h1><p>Controle de propostas, valores e validade.</p></div></div><div className="panel"><h2>Nova proposta</h2><form action={createProposal}><input name="title" required placeholder="Título da proposta"/> <select name="company_id"><option value="">Empresa</option>{companies.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select> <input name="total_value" type="number" min="0" step="0.01" placeholder="Valor"/> <input name="valid_until" type="date"/> <button type="submit">Criar proposta</button></form></div><div className="panel">{rows.length===0?<p className="empty">Nenhuma proposta cadastrada.</p>:<div className="table-wrap"><table><thead><tr><th>Nº</th><th>Proposta</th><th>Empresa</th><th>Valor</th><th>Validade</th><th>Status</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.number}</td><td><strong>{r.title}</strong></td><td>{r.company||'—'}</td><td>R$ {Number(r.total_value).toLocaleString('pt-BR',{minimumFractionDigits:2})}</td><td>{r.valid_until||'—'}</td><td>{r.status}</td></tr>)}</tbody></table></div>}</div></section>}
